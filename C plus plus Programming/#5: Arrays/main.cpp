#include <iostream>

using namespace std;

int main()
{
  int numbers1[] = {1, 2, 3, 4}; // declaring array of numbers
  int numbers2[4]; // declaring how many elements the variable can hold
  numbers2[0] = 1; // assigning the first element to one

cout <<"first integer is: "<< numbers1[0] << endl; // printing the first element of the first variable
cout <<"second integer is: "<< numbers2[0]; // printing the first element of the second variable
    return 0;
}
