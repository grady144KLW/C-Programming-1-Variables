#include <iostream>

using namespace std;

int main()
{
   int one;
   int two;

   cout << "first number ";
   cin >> one; // asking the user to enter the first number

   cout << "second number ";
   cin >> two; // asking the user to enter the second number

   cout << one + two; // adds the two numbers that the user has declared


    return 0;
}
